random.reseed
allocate.address 1024
chain.gaslimit 256

chain.deploy @1 RelayToken.gcl

log.highlight RelayToken test
log Perparing test transactions

state.set RelayToken.address @all { balance:"10000000000000" }

txn1[] = RelayToken.transfer*$~count$ @random { to:"$@random$", amount:"$random(1000, 2000)*100$" }

chain.info

stopwatch.restart
chain.run
stopwatch.report

chain.info

viz.profiling
